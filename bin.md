# `bin/rails` と `rails` の違い

```bash
bin/rails ......
rails ......
```

の違いが分からない

現場Rails 　P６５

> ```bash
> bin/rails db:create
> ```
>
> ここでは`rails`コマンドでは無く`bin/rails`コマンドを利用していることにお気づきでしょうか。これは、アプリケーションのルートディレクトリ直下の`bin`ディレクトリ にあるrailsというスクリプトを呼び出しています。このスクリプトを利用すると、`bundle exec rails`として実行した時と同様に、Gemfile通りのgemを利用できる環境上でコマンドを実行することが出来ます。
>
> > それに加えて、SpringというRailsの起動を効率的に行う機能も組み込まれます。このように、よく使うコマンドを包み込んで使いやすくするスクリプトを`binstub`といいます。

と「現場Rails」には書かれているが理解ができない。

> Gemfile通りのgemを利用出来る

とあるが

```bash
rails .....
```

コマンドでは出来ないのだろうか？

それとも

```bash
bundle
```

をしてからアプリを再起動する手間を省略するために行っているのだろうか

`bin`コマンドを付けないと付けないと挙動は変わるのだろうか

今までは付けてこなかったがやれてこれたので、

> 以降、本書では`bin/rails`にアクセスすることにします

との記述があるが、今のところその有益性が分かっていない

**ただ今のところ`bin/`を付けることで前に進まないとかが無いので理解の優先度が低い**