# `bundle exec`と`bundle`の違い　P８８

- ```bash
  bundle exec erb2slim app/views/layouts/ --delete
  bundle erb2slim app/views/layouts/ --delete
  ```

  上のコマンドとしたのコマンドの違いが分からない

- 単なる`bundle`と`bundle exec`はどう違う？